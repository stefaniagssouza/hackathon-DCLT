apiVersion: v1
kind: Service
metadata:
  name: ngo-service
  namespace: ngo
  labels:
    app: ngo-service
    app.kubernetes.io/name: ngo-service
    app.kubernetes.io/part-of: solidarytech
spec:
  selector:
    app: ngo-service
  ports:
    - name: http
      port: 80
      targetPort: http
